export const selectIsRefreshing = state => state.auth.isRefreshing;

export const selectIsLogin = state => state.auth;

export const selectIsLogOut = state => state.auth.user;

export const selectIsLoaggedIn = state => state.auth;
